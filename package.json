{
  "name": "bomby",
  "version": "1.0.0",
  "description": "![Build Status](https://gitlab.com/pages/neocities/badges/master/build.svg)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -g [ babelify --presets [ \"@babel/preset-env\" ] ] -g uglifyify --entry ./src/js/catmap.js > ./static/js/catmap.js && npm run hugo:build",
    "browserify" : "browserify -g [ babelify --presets [ \"@babel/preset-env\" ] ] -g uglifyify --entry ./src/js/catmap.js > ./static/js/catmap.js",
    "serve": "npm run hugo:serve",
    "hugo:build": "hugo -d build",
    "hugo:serve": "hugo server",
    "hugo:clean": "rm -rf build resources public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/wingywing/bomby.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/wingywing/bomby/issues"
  },
  "homepage": "https://gitlab.com/wingywing/bomby#readme",
  "dependencies": {
    "browserify-shim": "^3.8.14",
    "express": "^4.17.1",
    "hugo": "0.0.3",
    "ol": "^5.1.3",
    "save": "^2.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.56",
    "@babel/preset-env": "^7.0.0-beta.56",
    "babelify": "^9.0.0",
    "browserify": "^16.2.2",
    "uglifyify": "^5.0.1"
  }
}
